<Window x:Class="ETicaret.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:ETicaret.UI.Views" 
        Title="E-Ticaret Otomasyonu v1.0" 
        Height="1000" Width="1500"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen" 
        Background="#F1F5F9"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBlue" Color="#3B82F6"/>
        <SolidColorBrush x:Key="TextDark" Color="#1E293B"/>
        <SolidColorBrush x:Key="TextLight" Color="#64748B"/>
        <SolidColorBrush x:Key="BgLight" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="WhiteBrush" Color="#FFFFFF"/>

        <Style TargetType="TabControl">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Background="{StaticResource WhiteBrush}" 
                                    BorderThickness="0,0,0,1" BorderBrush="#E2E8F0" Padding="20,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,15,40,15">
                                        <Border Background="#EFF6FF" Width="40" Height="40" CornerRadius="8" Margin="0,0,12,0">
                                            <TextBlock Text="🚀" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
                                        </Border>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="E-Ticaret" FontWeight="Bold" FontSize="18" Foreground="{StaticResource TextDark}"/>
                                            <TextBlock Text="Otomasyonu" FontSize="12" Foreground="{StaticResource TextLight}"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <TabPanel Grid.Column="1" IsItemsHost="True" Background="Transparent" HorizontalAlignment="Left"/>
                                </Grid>
                            </Border>

                            <Border Grid.Row="1" Background="{TemplateBinding Background}" BorderThickness="0" CornerRadius="0">
                                <ContentPresenter ContentSource="SelectedContent"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid>
                            <Border Name="Border" Background="Transparent" Padding="25,20" Cursor="Hand" 
                                    BorderThickness="0,0,0,3" BorderBrush="Transparent">
                                <ContentPresenter x:Name="ContentSite"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Center"
                                                  ContentSource="Header"
                                                  RecognizesAccessKey="True"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBlue}"/>
                                <Setter Property="Foreground" Value="{StaticResource PrimaryBlue}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource PrimaryBlue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="FontSize" Value="15"/>
        </Style>
    </Window.Resources>

    <Grid>
        <TabControl>
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📦" Margin="0,0,8,0"/>
                        <TextBlock Text="Ürün Yönetimi"/>
                    </StackPanel>
                </TabItem.Header>
                <views:UrunlerView Margin="0,10,0,0"/>
            </TabItem>

            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="➕" Margin="0,0,8,0"/>
                        <TextBlock Text="Sipariş Oluştur"/>
                    </StackPanel>
                </TabItem.Header>
                <views:SiparisOlusturView Margin="0,10,0,0"/>
            </TabItem>

            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📋" Margin="0,0,8,0"/>
                        <TextBlock Text="Sipariş Geçmişi"/>
                    </StackPanel>
                </TabItem.Header>
                <views:SiparisDetayView Margin="0,10,0,0"/>
            </TabItem>

            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📊" Margin="0,0,8,0"/>
                        <TextBlock Text="Raporlar"/>
                    </StackPanel>
                </TabItem.Header>
                <views:RaporlarView Margin="0,10,0,0"/>
            </TabItem>

            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📊" Margin="0,0,8,0"/>
                        <TextBlock Text="İsteğe Göre Rapor"/>
                    </StackPanel>
                </TabItem.Header>
                <views:OzelRaporView Margin="0,10,0,0"/>
            </TabItem>

        </TabControl>
    </Grid>
</Window>