<Window x:Class="ETicaret.UI.Views.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SplashWindow" Height="350" Width="600"
        WindowStyle="None" ResizeMode="NoResize" 
        AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Border CornerRadius="20" Background="#2C3E50" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="10" Opacity="0.4"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="🚀" FontSize="60" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <TextBlock Text="E-TİCARET OTOMASYONU" 
                           Foreground="White" FontSize="28" FontWeight="Bold" 
                           HorizontalAlignment="Center"/>

                <TextBlock Text="v1.0.0" Foreground="#BDC3C7" FontSize="14" 
                           HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="0,40,0,0" Width="400">
                <TextBlock Text="Sistem Yükleniyor..." Foreground="#ECF0F1" 
                           FontSize="12" HorizontalAlignment="Center" Margin="0,0,0,5"/>

                <ProgressBar Height="6" IsIndeterminate="True" 
                             BorderThickness="0" Background="#34495E" Foreground="#3498DB">
                    <ProgressBar.Clip>
                        <RectangleGeometry Rect="0,0,400,6" RadiusX="3" RadiusY="3"/>
                    </ProgressBar.Clip>
                </ProgressBar>
            </StackPanel>
        </Grid>
    </Border>
</Window>