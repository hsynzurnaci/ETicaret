<UserControl x:Class="ETicaret.UI.Views.UrunlerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:ETicaret.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             FontFamily="Segoe UI" FontSize="14">

    <UserControl.DataContext>
        <vm:UrunlerViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryBlue" Color="#3B82F6"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#10B981"/>
        <SolidColorBrush x:Key="BackgroundLight" Color="#F0F9FF"/>
        <SolidColorBrush x:Key="SurfaceWhite" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextDark" Color="#1E293B"/>
        <SolidColorBrush x:Key="TextLight" Color="#64748B"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E2E8F0"/>

        <Style TargetType="TextBox" x:Key="ModernTextBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBlue}"/>
                                <Setter TargetName="border" Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox" x:Key="ModernComboBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="Button" x:Key="ModernButton">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource PrimaryBlue}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowHeight" Value="50"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="AlternatingRowBackground" Value="#F8FAFC"/>
            <Setter Property="Foreground" Value="{StaticResource TextDark}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="Foreground" Value="{StaticResource PrimaryBlue}"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Height" Value="45"/>
                        <Setter Property="Padding" Value="15,0"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="BorderThickness" Value="0,0,0,2"/>
                        <Setter Property="BorderBrush" Value="#E2E8F0"/>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="15,0"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                        <ContentPresenter VerticalAlignment="Center" Margin="15,0"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource BackgroundLight}"/>
                                <Setter Property="Foreground" Value="{StaticResource PrimaryBlue}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F1F5F9">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="25,25,25,15" Background="{StaticResource SurfaceWhite}" CornerRadius="16">
            <Border.Effect>
                <DropShadowEffect Color="#94A3B8" BlurRadius="25" ShadowDepth="0" Opacity="0.15"/>
            </Border.Effect>

            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="Kategori" Foreground="{StaticResource TextLight}" FontSize="12" Margin="0,0,0,6"/>
                        <ComboBox Style="{StaticResource ModernComboBox}" Width="160" 
                                  ItemsSource="{Binding Kategoriler}" SelectedItem="{Binding SecilenKategori}"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="Ürün Ara" Foreground="{StaticResource TextLight}" FontSize="12" Margin="0,0,0,6"/>
                        <TextBox Style="{StaticResource ModernTextBox}" Width="220" Text="{Binding AramaMetni}"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="Fiyat Aralığı (₺)" Foreground="{StaticResource TextLight}" FontSize="12" Margin="0,0,0,6"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBox Style="{StaticResource ModernTextBox}" Width="80" Text="{Binding MinFiyat}"/>
                            <TextBlock Text="-" VerticalAlignment="Center" Margin="8,0" Foreground="{StaticResource TextLight}"/>
                            <TextBox Style="{StaticResource ModernTextBox}" Width="80" Text="{Binding MaxFiyat}"/>
                        </StackPanel>
                    </StackPanel>

                    <Button Style="{StaticResource ModernButton}" Command="{Binding LoadUrunlerCommand}" 
                            Content="🔍 Filtrele" Width="120" VerticalAlignment="Bottom" Background="{StaticResource PrimaryBlue}"/>

                    <Border Background="{StaticResource BackgroundLight}" CornerRadius="10" Height="42" Padding="20,0" Margin="20,0,0,0" VerticalAlignment="Bottom">
                        <TextBlock Text="{Binding OrtalamaFiyatMetni}" Foreground="{StaticResource PrimaryBlue}" FontWeight="Bold" VerticalAlignment="Center"/>
                    </Border>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" >

                    <Button Style="{StaticResource ModernButton}" Command="{Binding FiyatGuncelleCommand}" 
            Background="White" Foreground="{StaticResource PrimaryBlue}" BorderBrush="{StaticResource PrimaryBlue}" BorderThickness="2"
            Width="180">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="" Margin="0,0,10,0"/>
                            <TextBlock Text="Fiyatları Güncelle"/>
                        </StackPanel>
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button Style="{StaticResource ModernButton}" Command="{Binding OpenIndirimDialogCommand}" 
            Background="White" Foreground="{StaticResource PrimaryBlue}" BorderBrush="{StaticResource PrimaryBlue}" BorderThickness="2"
            Width="180">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="" Margin="0,0,10,0"/>
                            <TextBlock Text="Kategori İndirimi"/>
                        </StackPanel>
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                </StackPanel>


            </Grid>
        </Border>



        <Grid Grid.Row="1" Margin="25,0,25,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.5*"/>
                <ColumnDefinition Width="25"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource SurfaceWhite}" CornerRadius="16" Padding="5">
                <Border.Effect>
                    <DropShadowEffect Color="#94A3B8" BlurRadius="25" ShadowDepth="0" Opacity="0.15"/>
                </Border.Effect>

                <DataGrid ItemsSource="{Binding Urunler}" IsReadOnly="True" SelectedItem="{Binding SecilenSatir}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Binding="{Binding UrunId}" Width="60"/>
                        <DataGridTextColumn Header="ÜRÜN ADI" Binding="{Binding UrunAdi}" Width="*"/>
                        <DataGridTextColumn Header="KATEGORİ" Binding="{Binding Kategori}" Width="150"/>
                        <DataGridTextColumn Header="FİYAT" Binding="{Binding FiyatFormatli}" Width="130" FontWeight="Bold"/>
                        <DataGridTextColumn Header="SATICI" Binding="{Binding SaticiId}" Width="90"/>

                        <DataGridTemplateColumn Header="İŞLEMLER" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Command="{Binding DataContext.AnalizEkleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding UrunId}"
                                            Content="Analiz Gir 📊"
                                            Background="#EFF6FF" Foreground="{StaticResource PrimaryBlue}"
                                            Height="32" FontSize="12" Margin="0" BorderThickness="0" Cursor="Hand" FontWeight="Bold">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="6"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Grid.Column="1" Background="{StaticResource SurfaceWhite}" CornerRadius="16" Padding="30" Grid.ColumnSpan="2" Margin="25,0,0,0">
                <Border.Effect>
                    <DropShadowEffect Color="#94A3B8" BlurRadius="25" ShadowDepth="0" Opacity="0.15"/>
                </Border.Effect>

                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
                        <Border Background="{StaticResource BackgroundLight}" Width="40" Height="40" CornerRadius="10">
                            <TextBlock Text="✏️" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="18"/>
                        </Border>
                        <StackPanel Margin="15,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Ürün Yönetimi" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextDark}"/>
                            <TextBlock Text="Düzenle veya Ekle" FontSize="12" Foreground="{StaticResource TextLight}"/>
                        </StackPanel>
                    </StackPanel>

                    <TextBlock Text="Ürün Adı" Foreground="{StaticResource TextLight}" FontSize="13" Margin="0,0,0,8"/>
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding FormUrun.UrunAdi}" Margin="0,0,0,20"/>

                    <TextBlock Text="Kategori" Foreground="{StaticResource TextLight}" FontSize="13" Margin="0,0,0,8"/>
                    <ComboBox Style="{StaticResource ModernComboBox}" ItemsSource="{Binding Kategoriler}" SelectedItem="{Binding FormUrun.Kategori}" Margin="0,0,0,20"/>

                    <TextBlock Text="Fiyat (₺)" Foreground="{StaticResource TextLight}" FontSize="13" Margin="0,0,0,8"/>
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding FormUrun.Fiyat}" Margin="0,0,0,40"/>
                    <TextBlock Text="Stok" Foreground="{StaticResource TextLight}" FontSize="13"/>
                    <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding FormUrun.StokAdedi}" Margin="0,0,0,40"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="109*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="87*"/>
                            <ColumnDefinition Width="22*"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0" Style="{StaticResource ModernButton}" Command="{Binding YeniKayitCommand}" 
                                Content="Temizle" Background="White" Foreground="{StaticResource TextLight}" 
                                BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Grid.ColumnSpan="2" Margin="8,-35,8,35"/>

                        <Button Grid.Column="2" Style="{StaticResource ModernButton}" Command="{Binding KaydetCommand}" 
                                Content="Kaydet ✓" Background="{StaticResource AccentGreen}" Margin="0,-35,0,35" Grid.ColumnSpan="2"/>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>