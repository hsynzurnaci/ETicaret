<UserControl x:Class="ETicaret.UI.Views.SiparisDetayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:ETicaret.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             FontFamily="Segoe UI" FontSize="14">

    <UserControl.DataContext>
        <vm:SiparisDetayViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryBlue" Color="#3B82F6"/>
        <SolidColorBrush x:Key="BackgroundLight" Color="#F0F9FF"/>
        <SolidColorBrush x:Key="SurfaceWhite" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextDark" Color="#1E293B"/>
        <SolidColorBrush x:Key="TextLight" Color="#64748B"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#10B981"/>

        <Style TargetType="TextBox" x:Key="ModernTextBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBlue}"/>
                                <Setter TargetName="border" Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="ModernButton">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource PrimaryBlue}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowHeight" Value="50"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="AlternatingRowBackground" Value="#F8FAFC"/>
            <Setter Property="Foreground" Value="{StaticResource TextDark}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="Foreground" Value="{StaticResource PrimaryBlue}"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Height" Value="45"/>
                        <Setter Property="Padding" Value="15,0"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="BorderThickness" Value="0,0,0,2"/>
                        <Setter Property="BorderBrush" Value="#E2E8F0"/>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="15,0"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                        <ContentPresenter VerticalAlignment="Center" Margin="15,0"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource BackgroundLight}"/>
                                <Setter Property="Foreground" Value="{StaticResource PrimaryBlue}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F1F5F9" Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="25,25,25,15" Background="{StaticResource SurfaceWhite}" CornerRadius="16">
            <Border.Effect>
                <DropShadowEffect Color="#94A3B8" BlurRadius="25" ShadowDepth="0" Opacity="0.15"/>
            </Border.Effect>

            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Sipariş No (ID):" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="{StaticResource TextDark}" Margin="0,0,15,0"/>

                <TextBox Grid.Column="1" Style="{StaticResource ModernTextBox}" 
                         Text="{Binding SiparisIdText, UpdateSourceTrigger=PropertyChanged}" 
                         Width="150" FontSize="16" FontWeight="Bold" Foreground="{StaticResource PrimaryBlue}"/>

                <Button Grid.Column="2" Style="{StaticResource ModernButton}" Command="{Binding GetirCommand}" 
                        Content="🔍 Siparişi Getir" Width="160" Margin="15,0,0,0"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Margin="25,0,25,25" Background="{StaticResource SurfaceWhite}" CornerRadius="16" Padding="30">
            <Border.Effect>
                <DropShadowEffect Color="#94A3B8" BlurRadius="25" ShadowDepth="0" Opacity="0.15"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,25">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Border Background="{StaticResource BackgroundLight}" Width="40" Height="40" CornerRadius="10">
                            <TextBlock Text="📋" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="18"/>
                        </Border>
                        <TextBlock Text="Sipariş Özeti" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextDark}" VerticalAlignment="Center" Margin="15,0,0,0"/>
                    </StackPanel>

                    <Border BorderThickness="0,0,0,1" BorderBrush="{StaticResource BorderColor}" Padding="0,0,0,15">
                        <StackPanel Orientation="Horizontal">
                            <StackPanel Margin="0,0,40,0">
                                <TextBlock Text="TARİH" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextLight}" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding SiparisDetay.Tarih}" FontSize="15" Foreground="{StaticResource TextDark}"/>
                            </StackPanel>

                            <StackPanel>
                                <TextBlock Text="MÜŞTERİ ID / ADI" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextLight}" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding SiparisDetay.SiparisId}" FontSize="15" Foreground="{StaticResource TextDark}" FontWeight="SemiBold"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <DataGrid Grid.Row="1" ItemsSource="{Binding SiparisDetay.Urunler}" 
                          IsReadOnly="True" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ÜRÜN ADI" Binding="{Binding UrunAdi}" Width="*"/>
                        <DataGridTextColumn Header="ADET" Binding="{Binding Miktar}" Width="100"/>
                        <DataGridTextColumn Header="BİRİM FİYAT" Binding="{Binding BirimFiyat,  StringFormat={}{0:N2} ₺}" Width="150"/>
                        <DataGridTextColumn Header="TOPLAM" Binding="{Binding ToplamTutar,  StringFormat={}{0:N2} ₺ }" Width="150" FontWeight="Bold" Foreground="{StaticResource PrimaryBlue}"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,25,0,0">
                    <TextBlock Text="GENEL TOPLAM:" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextLight}" VerticalAlignment="Center" Margin="0,0,15,0"/>

                    <Border Background="{StaticResource BackgroundLight}" CornerRadius="8" Padding="20,10">
                        <TextBlock Text="{Binding SiparisDetay.ToplamTutar,  StringFormat={}{0:N2} ₺}" FontSize="24" FontWeight="ExtraBold" Foreground="{StaticResource AccentGreen}"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>